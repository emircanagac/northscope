apiVersion: v1
kind: Service
metadata:
  name: {{ include "northscope.fullname" . }}
  labels:
    {{- include "northscope.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "northscope.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
