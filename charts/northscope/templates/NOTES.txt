NorthScope has been installed.

Release:
  {{ .Release.Name }} in namespace {{ .Release.Namespace }}

Port-forward:
  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "northscope.fullname" . }} 8080:{{ .Values.service.port }}

Open:
  http://localhost:8080

Uninstall:
  helm uninstall {{ .Release.Name }} -n {{ .Release.Namespace }}
